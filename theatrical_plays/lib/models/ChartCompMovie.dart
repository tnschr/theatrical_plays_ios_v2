import 'dart:ui';

class ChartCompMovie {
  final int? id;
  final String? title;
  final num priceRange;
  final String? labelTitle;
  final Color columnColor;

  ChartCompMovie(
      this.id, this.title, this.priceRange, this.labelTitle, this.columnColor);
}
